# Todo List

A single-file HTML todo list app for managing daily tasks. Built with vanilla HTML, CSS, and JavaScript — no frameworks, no server required.

## Features (Planned)
- Add, edit, and delete tasks
- Mark tasks as complete
- Filter by status (all / active / completed)
- Categories and priority levels
- Local storage persistence
- Search tasks
- Dark mode

## Usage
Open `index.html` in any browser.
